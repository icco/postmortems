---
uuid: 5cbc4e4d-d491-4576-a9fe-6f60ca05c9d8
url: https://web.archive.org/web/20211104160742/https://blog.cloudflare.com/how-and-why-the-leap-second-affected-cloudflare-dns/
title: Leap second affected Cloudflare DNS
start_time: 2017-01-01T00:00:00Z
end_time: 2017-01-01T06:45:00Z
categories:
- cascading-failure
- postmortem
- time
keywords:
- leap second
- cloudflare
- dns
- rrdns
- go
- cname
- time
- panic
company: Cloudflare
product: Cloudflare DNS
source_author: John Graham-Cumming
source_published_at: 2017-01-01T22:40:26Z
source_fetched_at: 2026-05-04T17:48:29.539321Z
summary: Backwards time flow from tracking [the 27th leap second on 2016-12-31T23:59:60Z](https://hpiers.obspm.fr/iers/bul/bulc/bulletinc.52) caused the weighted round-robin selection of DNS resolvers (RRDNS) to panic and fail on some CNAME lookups.  Go's `time.Now()` was incorrectly assumed to be monotonic; this injected negative values into calls to `rand.Int63n()`, which panics in that case.

---

On January 1, 2017, at midnight UTC, Cloudflare's custom RRDNS software experienced an issue due to the leap second. This led to negative values being generated in its internal calculations, which subsequently caused the RRDNS system to panic. The problem primarily affected DNS resolutions for customers utilizing CNAME DNS records.

The root cause was an incorrect assumption in the RRDNS code, written in Go, that time would always move forward monotonically. The `time.Now()` function, used to calculate Round Trip Times (RTT) for upstream DNS resolvers, returned a time earlier than a previously recorded start time immediately after the leap second. This resulted in negative RTT values.

These negative RTT values were smoothed over time and eventually fed into Go's `rand.Int63n()` function, which is designed to panic if its argument is negative. This panic in the weighted selection algorithm for upstream resolvers caused the RRDNS failures.

The customer impact was limited, affecting approximately 0.2% of DNS queries and less than 1% of HTTP requests at peak. The issue was quickly identified, and a fix was deployed within 90 minutes to the most affected machines.

The remediation involved a code change that prevented the recording of negative RTT values. This allowed RRDNS to normalize its performance metrics if time skipped backwards. The fix was rolled out worldwide by 06:45 UTC on January 1, 2017, resolving the incident.

